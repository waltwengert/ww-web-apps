import App from '../App';
import { UserInput } from '../components/UserInput';

const Generator = () => {
    return (
        <App>
            <UserInput />
        </App>
    );
};

export default Generator;
